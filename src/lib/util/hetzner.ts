/**
 * Convertes a Hetzner idenfier to a number.
 *
 * @param {string} id the identifier
 * @returns {number} the converted identifier
 */
export const hetznerIdentifierToNumber = (id: string): number => +id;
